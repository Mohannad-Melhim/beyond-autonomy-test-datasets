{
  "module_type": "DirectModule",
  "signature": {
    "description": "Classify whether a piece of text is safe or unsafe.",
    "input_fields": [
      {
        "name": "query",
        "type": "string",
        "description": "Input text to classify"
      }
    ],
    "output_fields": [
      {
        "name": "is_safe",
        "type": "boolean",
        "description": "Whether the text is safe"
      }
    ]
  }
}
