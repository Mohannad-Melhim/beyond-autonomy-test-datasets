{
  "instructions": "Classify whether the input text is safe or unsafe.",
  "signature": {
    "description": "Classify if the text is safe or not",
    "input_fields": [
      {
        "name": "query",
        "type": "string",
        "description": "Input text to classify"
      }
    ],
    "output_fields": [
      {
        "name": "is_safe",
        "type": "boolean",
        "description": "Safety classification"
      }
    ]
  }
}
